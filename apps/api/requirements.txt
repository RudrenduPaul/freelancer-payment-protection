fastapi==0.141.1
# fastapi's own declared starlette floor only reaches starlette>=0.46.0 (checked against
# fastapi 0.141.1's PyPI metadata, its latest release as of this bump); that's below the
# starlette>=1.3.1 needed to close every advisory pip-audit flagged, so it's pinned explicitly
# below. fastapi puts no upper bound on starlette, so this pin is safe to resolve.
starlette>=1.3.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
# bumped alongside fastapi/starlette above: fastapi==0.141.1 requires pydantic>=2.9.0
pydantic-settings==2.15.0
sqlalchemy==2.1.0
alembic==1.20.0
anthropic==1.8.0
httpx==0.28.1
python-multipart==0.0.32
slowapi==0.1.10
PyJWT==2.15.0
python-docx==1.2.0
weasyprint==70.0
celery==5.6.3
redis==8.1.0
email-validator==2.3.0

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "freelancer-payment-protection-cli"
dynamic = ["version"]
description = "Command-line client for freelancer-payment-protection: automated invoice escalation, legal document generation, and client risk scoring for freelancers."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Proprietary" }
authors = [
  { name = "Rudrendu Paul" },
  { name = "Sourav Nandy" },
]
keywords = ["invoicing", "freelance", "cli", "invoice-escalation", "risk-scoring", "agent-native", "supabase", "fastapi"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: Other/Proprietary License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business :: Financial :: Accounting",
  "Topic :: Utilities",
]
dependencies = [
  "click>=8.1,<9",
  "httpx>=0.27,<1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0,<9",
  "pytest-cov>=5.0,<7",
  "respx>=0.21,<1",
  "build>=1.0,<2",
  "twine>=5.0,<7",
]

[project.urls]
Homepage = "https://github.com/RudrenduPaul/freelancer-payment-protection"
Repository = "https://github.com/RudrenduPaul/freelancer-payment-protection"
"Bug Tracker" = "https://github.com/RudrenduPaul/freelancer-payment-protection/issues"

[project.scripts]
fpp = "freelancer_payment_protection_cli.main:main"
freelancer-payment-protection = "freelancer_payment_protection_cli.main:main"

[tool.hatch.version]
# Single source of truth for the package version: __version__ in
# __init__.py, which is also what `fpp --version` / click's
# version_option prints at runtime. Previously this file's own
# `version = "..."` field was bumped on every release but __init__.py's
# __version__ was never updated past its initial "0.1.0", so every
# published release (0.1.1 through 0.1.4 on PyPI) reported the wrong
# version at runtime despite installing correctly. Deriving the build
# version from this same file makes that drift structurally impossible.
path = "src/freelancer_payment_protection_cli/__init__.py"

[tool.hatch.build]
# This package lives in a subdirectory (packages/cli/) of a larger
# monorepo whose .git directory is at the repo root, alongside Node/
# Turborepo config that has nothing to do with this Python package.
ignore-vcs = true

[tool.hatch.build.targets.wheel]
packages = ["src/freelancer_payment_protection_cli"]

[tool.hatch.build.targets.sdist]
only-include = [
  "src/freelancer_payment_protection_cli",
  "tests",
  "README.md",
  "LICENSE",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
